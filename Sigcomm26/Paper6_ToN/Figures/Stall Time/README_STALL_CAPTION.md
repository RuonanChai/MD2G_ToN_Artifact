# Stall Time figures — CAPTION LOCK

These PDFs use `stall_last` = DECODE_GAP_SECONDS_POST_WARMUP from frozen COMMAND153
`dev.json` / `loot.json`.

They are **supporting continuity diagnostics only**.

Do NOT caption them as:
- "zero stall"
- "MoQ eliminates rebuffering"
- equivalent to SIGCOMM Fig. 8 DASH multi-second stall comparison

Primary paper claims use system utility U and matched ΔU.
See `../STALL_VS_SIGCOMM_PAPER.md`.
