# TTFB CDF — FAIL CLOSED

perf.csv columns ttfb_* and delay_ms exist but are unimplemented zeros across audited VALID canary (all cells) and MAINDEV sample (300). No truthful CDF can be drawn.

See `EVIDENCE_GAP_TTFB.json`.
